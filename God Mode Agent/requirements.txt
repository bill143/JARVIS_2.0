# Core backend dependencies (always required)
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
python-dotenv>=1.0

# Phase 2 core (all stdlib-based auth/policy/observability need no extra runtime deps;
# these enrich optional integrations and are safe to require)
# (JWT, password hashing, metrics text format, and audit hash-chain use stdlib only.)

# Test + lint toolchain
pytest>=8.2
pytest-asyncio>=0.23
ruff>=0.5
